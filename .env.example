# Chrome MCP Server Configuration

# Server Port
CHROME_MCP_PORT=8080

# Chrome DevTools Protocol Port
CHROME_CDP_PORT=9222

# Logging Level (debug, info, warn, error)
LOG_LEVEL=info

# Database Path (optional, defaults to ./sessions.db)
DB_PATH=./sessions.db

# CORS Settings (optional)
CORS_ORIGINS=*

# Agent Configuration
ANTHROPIC_API_KEY=your-anthropic-api-key-here
OPENAI_API_KEY=your-openai-api-key-here

# Session Limits
MAX_SESSIONS=10
SESSION_TIMEOUT=3600000

# Chrome Binary Path (optional, auto-detected if not set)
# CHROME_BINARY_PATH=/path/to/chrome

# Native Messaging Host Configuration
NATIVE_MESSAGING_HOST_NAME=com.mcp.chrome.bridge